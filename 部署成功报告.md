# 医院指标管理系统 - 部署成功报告

## 部署信息

**部署时间**: 2026-01-10 13:56:00
**服务器地址**: 81.71.44.180
**部署目录**: /opt/indicator-management
**应用进程 PID**: 24054

## 部署内容

### 1. 应用文件
- **JAR包**: indicator-management-1.0.0-SNAPSHOT.jar (36MB)
- **启动脚本**: start.sh, stop.sh, restart.sh
- **日志目录**: logs/

### 2. 应用配置
- **端口**: 8080
- **上下文路径**: /dgear
- **JVM 参数**:
  - 初始内存: 512MB
  - 最大内存: 1024MB
  - 堆转储: logs/heap_dump.hprof
  - 文件编码: UTF-8
  - 时区: GMT+08

### 3. 日志配置
- **应用日志**: logs/application.log
- **控制台日志**: logs/console.log
- **日志级别**:
  - root: WARN
  - com.hospital.indicator: WARN
  - com.hospital.indicator.mapper: DEBUG (打印SQL)
- **日志轮转**:
  - 单文件最大: 100MB
  - 保留天数: 30天
  - 总容量上限: 3GB

## 应用状态

### 进程状态
```
root     24054 77.3  2.8 5226056 458612 ?  Sl 13:56 java -jar indicator-management...
```
✅ 应用正在运行

### 端口监听
```
tcp6  0  0  :::8080  :::*  LISTEN  24054/java
```
✅ 端口 8080 已监听

## 访问地址

### Swagger API 文档
```
http://81.71.44.180:8080/dgear/swagger-ui.html
```

### API 端点示例
```
# 指标树查询
GET http://81.71.44.180:8080/dgear/api/indicator/tree

# 指标计算
POST http://81.71.44.180:8080/dgear/api/indicator-calculation/calculate
```

## 运维命令

### 查看日志
```bash
# 查看应用日志（实时）
ssh root@81.71.44.180
tail -f /opt/indicator-management/logs/application.log

# 查看控制台输出
tail -f /opt/indicator-management/logs/console.log

# 查看最近50行日志
tail -50 /opt/indicator-management/logs/application.log
```

### 应用控制
```bash
# 连接服务器
ssh root@81.71.44.180
cd /opt/indicator-management

# 停止应用
./stop.sh

# 启动应用
./start.sh

# 重启应用
./restart.sh

# 查看进程状态
ps aux | grep java | grep indicator-management

# 查看端口监听
netstat -tlnp | grep :8080
```

### 重新部署
```bash
# 在本地运行
python deploy_to_server.py
```

## 部署文件清单

本地部署包位置: `deploy/release/`

```
deploy/release/
├── indicator-management-1.0.0-SNAPSHOT.jar   (36MB)
├── start.sh                                   (1.1KB)
├── stop.sh                                    (806B)
└── restart.sh                                 (169B)
```

## 注意事项

1. **日志持续输出**:
   - 应用日志会持续写入 `logs/application.log`
   - SQL 语句在 DEBUG 级别下会输出
   - 错误信息会完整记录到日志文件

2. **性能监控**:
   - 当前内存占用: ~450MB (初始512MB, 最大1024MB)
   - CPU 使用率正常

3. **后续维护**:
   - 定期检查日志文件大小（自动轮转）
   - 监控应用内存使用情况
   - 建议配置防火墙规则限制8080端口访问

## 部署脚本说明

### 自动化部署脚本
- **deploy_to_server.py**: Python 自动部署脚本
  - 自动连接服务器
  - 上传文件
  - 停止旧应用
  - 启动新应用

### 手动部署方法
如果自动脚本失败，可以手动执行：
```bash
# 1. 上传文件
scp deploy/release/* root@81.71.44.180:/opt/indicator-management/

# 2. 连接服务器
ssh root@81.71.44.180

# 3. 修复换行符并启动
cd /opt/indicator-management
dos2unix *.sh
chmod +x *.sh
./start.sh
```

## 部署成功标志

✅ 所有部署步骤已完成
✅ 应用进程正常运行 (PID: 24054)
✅ 端口 8080 正在监听
✅ 日志文件正常生成
✅ 日志持续输出配置已生效
