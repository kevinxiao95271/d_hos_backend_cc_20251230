# 日志配置说明 - 按天自动新建日志文件

## 📋 日志文件列表

服务器日志目录: `/opt/indicator-management/logs/`

### 1. 应用主日志（按天滚动）
```
logs/application.log                    # 当前日志文件
logs/application-2026-01-10.0.log      # 今天的归档（当文件>100MB时）
logs/application-2026-01-09.0.log      # 昨天的日志
logs/application-2026-01-09.1.log      # 昨天的第2个文件（>100MB后分割）
...
```

**特点**:
- ✅ 每天自动创建新文件
- ✅ 单个文件超过 100MB 自动分割
- ✅ 保留 30 天
- ✅ 总大小上限 3GB

### 2. SQL 执行日志（按天滚动）
```
logs/sql.log                           # 当前 SQL 日志
logs/sql-2026-01-10.0.log             # 今天的 SQL 归档
logs/sql-2026-01-09.0.log             # 昨天的 SQL 日志
...
```

**特点**:
- ✅ 单独记录所有 SQL 语句
- ✅ 方便排查数据库查询问题
- ✅ 单文件 50MB 自动分割
- ✅ 保留 7 天
- ✅ 总大小上限 500MB

### 3. 错误日志（按天滚动）
```
logs/error.log                         # 当前错误日志
logs/error-2026-01-10.0.log           # 今天的错误归档
logs/error-2026-01-09.0.log           # 昨天的错误日志
...
```

**特点**:
- ✅ 只记录 ERROR 级别日志
- ✅ 快速定位错误问题
- ✅ 单文件 50MB 自动分割
- ✅ 保留 30 天
- ✅ 总大小上限 1GB

### 4. 控制台输出日志
```
logs/console.log                       # 启动信息和标准输出
```

## 🔍 日志查看命令

### 查看实时日志
```bash
# 应用日志（实时）
tail -f /opt/indicator-management/logs/application.log

# SQL 日志（实时）
tail -f /opt/indicator-management/logs/sql.log

# 错误日志（实时）
tail -f /opt/indicator-management/logs/error.log
```

### 查看今天的日志
```bash
# 查看今天所有应用日志
cat /opt/indicator-management/logs/application-$(date +%Y-%m-%d).*.log

# 查看今天所有 SQL 日志
cat /opt/indicator-management/logs/sql-$(date +%Y-%m-%d).*.log

# 查看今天所有错误日志
cat /opt/indicator-management/logs/error-$(date +%Y-%m-%d).*.log
```

### 查看昨天的日志
```bash
# 查看昨天的应用日志
cat /opt/indicator-management/logs/application-$(date -d yesterday +%Y-%m-%d).*.log
```

### 查看最近N行
```bash
# 最近100行应用日志
tail -100 /opt/indicator-management/logs/application.log

# 最近50行 SQL 日志
tail -50 /opt/indicator-management/logs/sql.log

# 最近30行错误日志
tail -30 /opt/indicator-management/logs/error.log
```

### 搜索关键字
```bash
# 在今天的日志中搜索关键字
grep "ERROR" /opt/indicator-management/logs/application-$(date +%Y-%m-%d).*.log

# 在所有日志中搜索
grep -r "指标计算失败" /opt/indicator-management/logs/

# 查看某个时间段的日志
grep "13:00:00" /opt/indicator-management/logs/application.log
```

## 📊 日志格式说明

### 应用日志格式
```
2026-01-10 14:01:23.456 [http-nio-8080-exec-1] WARN  c.h.i.s.i.IndicatorCalculationServiceImpl - 指标计算失败: metricCode=10.3.1
```

**格式**: `日期 时间 [线程名] 级别 类名 - 消息`

### SQL 日志格式
```
2026-01-10 14:01:23.456 - ==>  Preparing: SELECT * FROM t_indicator WHERE metric_code = ?
2026-01-10 14:01:23.457 - ==> Parameters: 10.3.1(String)
2026-01-10 14:01:23.458 - <==      Total: 1
```

**格式**: `日期 时间 - SQL内容`

## 🔧 日志级别说明

| 级别  | 用途 | 示例 | 是否输出 |
|------|------|------|---------|
| DEBUG | SQL 语句、Mapper 执行 | SQL 查询日志 | ✅ 仅 SQL 日志 |
| INFO  | 普通信息（已禁用） | 启动信息 | ❌ 不输出 |
| WARN  | 警告信息 | 除零警告、参数校验警告 | ✅ 输出 |
| ERROR | 错误信息 | 异常堆栈、业务错误 | ✅ 输出到所有日志 |

## ⚙️ 自动清理机制

### 应用日志
- 保留 30 天的日志文件
- 超过 30 天的文件自动删除
- 总大小超过 3GB 时，删除最旧的文件

### SQL 日志
- 保留 7 天的 SQL 日志
- 超过 7 天自动删除
- 总大小超过 500MB 时，删除最旧的文件

### 错误日志
- 保留 30 天的错误日志
- 超过 30 天自动删除
- 总大小超过 1GB 时，删除最旧的文件

## 📁 日志文件命名规则

```
logs/
├── application.log                      # 当前应用日志（符号链接）
├── application-2026-01-10.0.log        # 日期.序号.log
├── application-2026-01-10.1.log        # 同一天第2个文件（>100MB后）
├── application-2026-01-09.0.log        # 昨天的日志
│
├── sql.log                              # 当前 SQL 日志
├── sql-2026-01-10.0.log                # 今天的 SQL 日志
│
├── error.log                            # 当前错误日志
├── error-2026-01-10.0.log              # 今天的错误日志
│
└── console.log                          # 控制台输出（启动脚本重定向）
```

## 🎯 常见问题定位

### 1. 查找今天的错误
```bash
cat /opt/indicator-management/logs/error-$(date +%Y-%m-%d).*.log
```

### 2. 查找某个指标的计算日志
```bash
grep "metricCode=10.3.1" /opt/indicator-management/logs/application-$(date +%Y-%m-%d).*.log
```

### 3. 查看某个时间段的 SQL
```bash
# 查看 14:00 到 15:00 之间的 SQL
sed -n '/14:00:00/,/15:00:00/p' /opt/indicator-management/logs/sql-$(date +%Y-%m-%d).0.log
```

### 4. 统计今天的错误次数
```bash
grep -c "ERROR" /opt/indicator-management/logs/error-$(date +%Y-%m-%d).*.log
```

### 5. 查看最近启动的日志
```bash
tail -100 /opt/indicator-management/logs/console.log
```

## 📈 性能优化

### 异步日志输出
- 使用 AsyncAppender 提高日志写入性能
- 队列大小: 512（应用日志），256（错误日志）
- 不丢弃任何日志（discardingThreshold=0）

### 日志压缩
如需节省磁盘空间，可以手动压缩旧日志：
```bash
cd /opt/indicator-management/logs
gzip application-2026-01-09.*.log
gzip sql-2026-01-08.*.log
```

## 🔒 日志安全

- 日志文件权限: 644（只有 root 可写）
- 日志目录权限: 755
- 敏感信息已脱敏（数据库密码等不会输出）

## 📝 配置文件位置

- **详细配置**: `src/main/resources/logback-spring.xml`
- **应用配置**: `src/main/resources/application.yml`

## ✅ 优化效果

- ✅ 按天自动创建新日志文件，方便按日期查找
- ✅ 日志文件自动分割，单文件不会过大
- ✅ SQL 单独记录，方便排查数据库问题
- ✅ 错误日志单独输出，快速定位错误
- ✅ 自动清理过期日志，节省磁盘空间
- ✅ 异步写入，不影响应用性能
