# 指标树形结构管理说明

## 1. 指标节点类型

### 1.1 非叶子节点（分类节点）
**作用**：用于组织和分类指标，构建树形层级结构

**特征**：
- `isLeaf = 0`
- `calculationType = "NONE"` 或不填
- 不包含 `expression` 和 `relatedItems`
- **不能**执行指标计算
- 可以有子节点

**示例场景**：
```
医疗质量控制指标 (分类节点)
├── 重点病种质量控制指标 (分类节点)
│   ├── 急性心肌梗死平均住院日 (叶子节点，可计算)
│   └── 心力衰竭平均住院日 (叶子节点，可计算)
└── 手术质量控制指标 (分类节点)
    ├── 手术患者并发症发生率 (叶子节点，可计算)
    └── I类切口感染率 (叶子节点，可计算)
```

### 1.2 叶子节点（可计算指标）
**作用**：实际的业务指标，包含计算逻辑

**特征**：
- `isLeaf = 1`
- `calculationType = "ITEM"` 或 `"EXPRESSION"`
- 包含 `expression` 和 `relatedItems`
- **可以**执行指标计算
- **不能**有子节点

---

## 2. 新增接口用法对比

### 2.1 新增分类节点（非叶子节点）

**接口**: `POST /api/indicator`

**请求示例**:
```javascript
// 新增顶级分类节点
fetch('http://localhost:8080/dgear/api/indicator', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '医疗质量控制指标',
    metricName: '医疗质量控制指标',
    parentCode: null,              // 顶级节点，无父节点
    indicatorLevel: 1,             // 第一层级
    isLeaf: 0,                     // 非叶子节点
    metricType: 'QUANTITATIVE',
    calculationType: 'NONE',       // 分类节点不计算
    status: 1,
    sortOrder: 1
  })
})
.then(res => res.json())
.then(data => console.log(data));
```

```javascript
// 新增二级分类节点
fetch('http://localhost:8080/dgear/api/indicator', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '重点病种质量控制指标',
    metricName: '重点病种质量控制指标',
    parentCode: '医疗质量控制指标',  // 指定父节点
    indicatorLevel: 2,               // 第二层级
    isLeaf: 0,                       // 非叶子节点
    metricType: 'QUANTITATIVE',
    calculationType: 'NONE',
    status: 1,
    sortOrder: 1
  })
})
.then(res => res.json())
.then(data => console.log(data));
```

### 2.2 新增叶子节点（可计算指标）

**接口**: `POST /api/indicator`

**请求示例**:
```javascript
// 新增可计算的叶子指标
fetch('http://localhost:8080/dgear/api/indicator', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '急性心肌梗死平均住院日',
    metricName: '急性心肌梗死平均住院日',
    parentCode: '重点病种质量控制指标',  // 父节点是分类节点
    indicatorLevel: 3,                    // 第三层级
    isLeaf: 1,                            // 叶子节点
    metricType: 'QUANTITATIVE',
    calculationType: 'EXPRESSION',        // 使用表达式计算
    expression: 'a0034/a0032',           // 计算表达式
    relatedItems: '["a0034","a0032"]',   // 关联的指标项
    unit: '天',
    supportDeptDrill: 1,                 // 支持科室下钻
    status: 1,
    sortOrder: 1
  })
})
.then(res => res.json())
.then(data => console.log(data));
```

---

## 3. 字段对比表

| 字段 | 分类节点 | 叶子节点 | 说明 |
|------|----------|----------|------|
| `metricCode` | ✅ 必填 | ✅ 必填 | 指标编码 |
| `metricName` | ✅ 必填 | ✅ 必填 | 指标名称 |
| `parentCode` | 可选 | ✅ 建议填 | 父节点编码，顶级节点为null |
| `indicatorLevel` | ✅ 必填 | ✅ 必填 | 层级：1-顶级，2-二级... |
| `isLeaf` | 0 | 1 | 是否叶子节点 |
| `metricType` | ✅ 必填 | ✅ 必填 | QUANTITATIVE/QUALITATIVE |
| `calculationType` | NONE | ITEM/EXPRESSION | 计算类型 |
| `expression` | ❌ 不填 | ✅ 必填 | 计算表达式 |
| `relatedItems` | ❌ 不填 | ✅ 必填 | 关联指标项JSON数组 |
| `unit` | 可选 | ✅ 建议填 | 单位 |
| `supportDeptDrill` | ❌ 一般为0 | 可选 | 是否支持科室下钻 |
| `status` | ✅ 必填 | ✅ 必填 | 0-禁用，1-启用 |
| `sortOrder` | 可选 | 可选 | 排序号 |

---

## 4. 查询接口用法

### 4.1 查询指标树（包含所有节点）

**接口**: `GET /api/indicator/tree`

**说明**: 返回完整的树形结构，包括分类节点和叶子节点

**请求示例**:
```javascript
fetch('http://localhost:8080/dgear/api/indicator/tree')
  .then(res => res.json())
  .then(data => {
    console.log('指标树:', data.data);
    // data.data 是树形结构数组
  });
```

**响应示例**:
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "metricCode": "医疗质量控制指标",
      "metricName": "医疗质量控制指标",
      "isLeaf": 0,
      "indicatorLevel": 1,
      "children": [
        {
          "id": 2,
          "metricCode": "重点病种质量控制指标",
          "metricName": "重点病种质量控制指标",
          "isLeaf": 0,
          "indicatorLevel": 2,
          "children": [
            {
              "id": 3,
              "metricCode": "急性心肌梗死平均住院日",
              "metricName": "急性心肌梗死平均住院日",
              "isLeaf": 1,
              "indicatorLevel": 3,
              "expression": "a0034/a0032",
              "unit": "天",
              "children": []
            }
          ]
        }
      ]
    }
  ]
}
```

### 4.2 查询子节点

**接口**: `GET /api/indicator/children/{parentCode}`

**说明**: 查询指定父节点下的所有直接子节点（包括分类节点和叶子节点）

**请求示例**:
```javascript
// 查询"重点病种质量控制指标"下的所有子指标
fetch('http://localhost:8080/dgear/api/indicator/children/重点病种质量控制指标')
  .then(res => res.json())
  .then(data => console.log(data));
```

### 4.3 分页查询（可按节点类型过滤）

**接口**: `GET /api/indicator/page`

**请求示例**:
```javascript
// 只查询叶子节点（可计算指标）
const params = new URLSearchParams({
  current: 1,
  size: 10,
  isLeaf: 1  // 过滤条件：只查询叶子节点
});

fetch(`http://localhost:8080/dgear/api/indicator/page?${params}`)
  .then(res => res.json())
  .then(data => console.log('叶子指标列表:', data));
```

```javascript
// 只查询分类节点
const params = new URLSearchParams({
  current: 1,
  size: 10,
  isLeaf: 0  // 过滤条件：只查询分类节点
});

fetch(`http://localhost:8080/dgear/api/indicator/page?${params}`)
  .then(res => res.json())
  .then(data => console.log('分类节点列表:', data));
```

---

## 5. 更新接口用法

### 5.1 更新分类节点

**接口**: `PUT /api/indicator/{id}`

**请求示例**:
```javascript
// 更新分类节点名称
fetch('http://localhost:8080/dgear/api/indicator/1', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '医疗质量控制指标',
    metricName: '医疗质量控制指标（2025版）', // 修改名称
    parentCode: null,
    indicatorLevel: 1,
    isLeaf: 0,
    metricType: 'QUANTITATIVE',
    calculationType: 'NONE',
    status: 1,
    sortOrder: 1
  })
})
.then(res => res.json())
.then(data => console.log(data));
```

### 5.2 更新叶子节点

**接口**: `PUT /api/indicator/{id}`

**请求示例**:
```javascript
// 更新叶子指标的计算表达式
fetch('http://localhost:8080/dgear/api/indicator/3', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '急性心肌梗死平均住院日',
    metricName: '急性心肌梗死平均住院日',
    parentCode: '重点病种质量控制指标',
    indicatorLevel: 3,
    isLeaf: 1,
    metricType: 'QUANTITATIVE',
    calculationType: 'EXPRESSION',
    expression: 'SUM(a0034)/SUM(a0032)',  // 修改表达式
    relatedItems: '["a0034","a0032"]',
    unit: '天',
    supportDeptDrill: 1,
    status: 1,
    sortOrder: 1
  })
})
.then(res => res.json())
.then(data => console.log(data));
```

---

## 6. 删除接口用法

### 6.1 删除节点的规则

**重要**：删除节点时需要注意级联关系

1. **删除叶子节点**：直接删除，不影响其他节点
2. **删除分类节点**：
   - 如果有子节点，建议先删除或转移子节点
   - 系统可能不允许直接删除有子节点的分类节点（取决于业务规则）

**接口**: `DELETE /api/indicator/{id}`

**请求示例**:
```javascript
// 删除叶子节点
fetch('http://localhost:8080/dgear/api/indicator/3', {
  method: 'DELETE'
})
.then(res => res.json())
.then(data => console.log(data));
```

---

## 7. 指标计算接口（仅针对叶子节点）

### 7.1 执行指标计算

**重要**：只有叶子节点可以执行计算，分类节点不能计算

**接口**: `POST /api/indicator-result/calculate`

**请求示例**:
```javascript
// ✅ 正确：对叶子指标执行计算
const params = new URLSearchParams({
  metricCode: '急性心肌梗死平均住院日',  // 叶子节点
  timeDimension: 'YEAR',
  startDate: '2023-01-01',
  endDate: '2023-12-31'
});

fetch(`http://localhost:8080/dgear/api/indicator-result/calculate?${params}`, {
  method: 'POST'
})
.then(res => res.json())
.then(data => console.log('计算成功:', data));
```

```javascript
// ❌ 错误：对分类节点执行计算会失败
const params = new URLSearchParams({
  metricCode: '重点病种质量控制指标',  // 分类节点
  timeDimension: 'YEAR',
  startDate: '2023-01-01',
  endDate: '2023-12-31'
});

fetch(`http://localhost:8080/dgear/api/indicator-result/calculate?${params}`, {
  method: 'POST'
})
.then(res => res.json())
.then(data => {
  // 会返回错误：只能计算叶子节点指标
  console.error('计算失败:', data.message);
});
```

### 7.2 批量计算（自动过滤叶子节点）

**接口**: `POST /api/indicator-result/batch-calculate`

**说明**: 传入空数组时，系统会自动查询所有叶子节点进行计算

**请求示例**:
```javascript
// 计算所有叶子指标（系统自动过滤分类节点）
fetch('http://localhost:8080/dgear/api/indicator-result/batch-calculate?timeDimension=YEAR&startDate=2023-01-01&endDate=2023-12-31', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify([])  // 空数组，系统会自动找所有 isLeaf=1 的指标
})
.then(res => res.json())
.then(data => console.log(data));
```

```javascript
// 计算指定的多个叶子指标
fetch('http://localhost:8080/dgear/api/indicator-result/batch-calculate?timeDimension=MONTH&startDate=2023-01-01&endDate=2023-03-31', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify([
    '急性心肌梗死平均住院日',    // 叶子节点
    '心力衰竭平均住院日',        // 叶子节点
    '肺炎（住院、成人）平均住院日' // 叶子节点
  ])
})
.then(res => res.json())
.then(data => console.log(data));
```

---

## 8. 前端展示建议

### 8.1 树形组件展示

**Vue3 + Element Plus 示例**:

```vue
<template>
  <el-tree
    :data="treeData"
    :props="treeProps"
    node-key="id"
    :default-expand-all="false"
  >
    <template #default="{ node, data }">
      <span class="custom-tree-node">
        <!-- 分类节点显示文件夹图标 -->
        <el-icon v-if="data.isLeaf === 0">
          <Folder />
        </el-icon>
        <!-- 叶子节点显示文档图标 -->
        <el-icon v-else>
          <Document />
        </el-icon>

        <span>{{ data.metricName }}</span>

        <!-- 叶子节点显示标签 -->
        <el-tag v-if="data.isLeaf === 1" size="small" type="success">
          可计算
        </el-tag>

        <!-- 显示单位 -->
        <span v-if="data.unit" class="unit">{{ data.unit }}</span>
      </span>
    </template>
  </el-tree>
</template>

<script setup>
import { ref, onMounted } from 'vue';
import { Folder, Document } from '@element-plus/icons-vue';

const treeData = ref([]);
const treeProps = {
  children: 'children',
  label: 'metricName'
};

const loadTree = async () => {
  const response = await fetch('http://localhost:8080/dgear/api/indicator/tree');
  const result = await response.json();
  if (result.code === 200) {
    treeData.value = result.data;
  }
};

onMounted(() => {
  loadTree();
});
</script>

<style scoped>
.custom-tree-node {
  display: flex;
  align-items: center;
  gap: 8px;
}

.unit {
  color: #909399;
  font-size: 12px;
}
</style>
```

### 8.2 右键菜单功能差异

根据节点类型显示不同的操作菜单：

**分类节点菜单**：
- 编辑
- 新增子分类
- 新增子指标
- 删除（需检查是否有子节点）

**叶子节点菜单**：
- 编辑
- 执行计算
- 查看计算结果
- 删除

```vue
<template>
  <el-tree
    :data="treeData"
    @node-contextmenu="handleContextMenu"
  >
    <!-- 树形节点内容 -->
  </el-tree>

  <!-- 右键菜单 -->
  <el-dropdown trigger="contextmenu">
    <template v-if="currentNode.isLeaf === 0">
      <!-- 分类节点菜单 -->
      <el-dropdown-item @click="handleEdit">编辑分类</el-dropdown-item>
      <el-dropdown-item @click="handleAddChild">新增子分类</el-dropdown-item>
      <el-dropdown-item @click="handleAddIndicator">新增子指标</el-dropdown-item>
      <el-dropdown-item divided @click="handleDelete">删除</el-dropdown-item>
    </template>
    <template v-else>
      <!-- 叶子节点菜单 -->
      <el-dropdown-item @click="handleEdit">编辑指标</el-dropdown-item>
      <el-dropdown-item @click="handleCalculate">执行计算</el-dropdown-item>
      <el-dropdown-item @click="handleViewResult">查看结果</el-dropdown-item>
      <el-dropdown-item divided @click="handleDelete">删除</el-dropdown-item>
    </template>
  </el-dropdown>
</template>
```

---

## 9. 完整示例：构建指标树

### 9.1 创建完整的三级指标树

```javascript
// 步骤1: 创建顶级分类
const level1 = await fetch('http://localhost:8080/dgear/api/indicator', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '医疗质量控制指标',
    metricName: '医疗质量控制指标',
    parentCode: null,
    indicatorLevel: 1,
    isLeaf: 0,
    metricType: 'QUANTITATIVE',
    calculationType: 'NONE',
    status: 1
  })
}).then(res => res.json());

console.log('创建顶级分类:', level1);

// 步骤2: 创建二级分类
const level2 = await fetch('http://localhost:8080/dgear/api/indicator', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '重点病种质量控制指标',
    metricName: '重点病种质量控制指标',
    parentCode: '医疗质量控制指标',
    indicatorLevel: 2,
    isLeaf: 0,
    metricType: 'QUANTITATIVE',
    calculationType: 'NONE',
    status: 1
  })
}).then(res => res.json());

console.log('创建二级分类:', level2);

// 步骤3: 创建叶子指标
const level3 = await fetch('http://localhost:8080/dgear/api/indicator', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '急性心肌梗死平均住院日',
    metricName: '急性心肌梗死平均住院日',
    parentCode: '重点病种质量控制指标',
    indicatorLevel: 3,
    isLeaf: 1,                           // 叶子节点
    metricType: 'QUANTITATIVE',
    calculationType: 'EXPRESSION',        // 有计算逻辑
    expression: 'a0034/a0032',
    relatedItems: '["a0034","a0032"]',
    unit: '天',
    supportDeptDrill: 1,
    status: 1
  })
}).then(res => res.json());

console.log('创建叶子指标:', level3);

// 步骤4: 查看完整树形结构
const tree = await fetch('http://localhost:8080/dgear/api/indicator/tree')
  .then(res => res.json());

console.log('完整指标树:', tree);
```

---

## 10. 常见问题

### Q1: 可以把叶子节点改成分类节点吗？
**答**: 不建议。如果需要，应该：
1. 先删除或转移该节点下的计算结果
2. 清空 `expression` 和 `relatedItems`
3. 将 `isLeaf` 改为 0
4. 将 `calculationType` 改为 `NONE`

### Q2: 分类节点可以有多少层？
**答**: 理论上无限制，但建议不超过5层，保持树形结构清晰。

### Q3: 如何快速找到所有可计算的指标？
**答**: 使用分页查询接口，过滤 `isLeaf=1`：
```javascript
fetch('http://localhost:8080/dgear/api/indicator/page?isLeaf=1')
  .then(res => res.json())
  .then(data => console.log('所有叶子指标:', data));
```

### Q4: 删除分类节点会删除其子节点吗？
**答**: 这取决于后端的实现逻辑。建议在删除前检查是否有子节点，提示用户先处理子节点。

---

**文档版本**: 1.0.0
**最后更新**: 2026-01-08
