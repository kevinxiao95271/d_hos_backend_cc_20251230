# 科室下钻结果全零问题修复报告

## 问题描述

用户反馈：指标"成人患者数"的普通计算结果为84人（2023年1月），但科室下钻返回的所有科室结果都是0。

## 问题分析

### 根本原因

**指标项SQL缺少结果列别名！**

#### 错误的SQL（C0002）
```sql
select count(distinct A48,A49)
from d_mr
where A14 >= 18
AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN #{startDate} AND #{endDate}
```

#### 问题点
1. `COUNT(distinct A48,A49)` 没有使用 `AS result_value` 别名
2. 科室下钻时，后端会修改SQL为：
   ```sql
   SELECT B16 as dept_code, B16 as dept_name, count(distinct A48,A49)
   FROM d_mr
   WHERE ...
   GROUP BY B16
   ```
3. 返回的列名是 `count(distinct A48,A49)` 而不是 `result_value`
4. 后端代码读取时使用的是：
   ```java
   Object valueObj = row.get("result_value");  // 第547行
   ```
5. 因为找不到`result_value`列，`valueObj`为null
6. null值被转换为0（第560行）

### 代码位置

**文件**: `IndicatorCalculationServiceImpl.java`

**关键代码** (第547-560行):
```java
// 5. 解析查询结果
for (Map<String, Object> row : rows) {
    // 获取科室编码和科室名称
    String deptCode = getStringValue(row.get("dept_code"));
    String deptName = getStringValue(row.get("dept_name"));
    Object valueObj = row.get("result_value");  // ← 这里读取result_value

    // ...

    // 转换值为BigDecimal
    BigDecimal value;
    if (valueObj == null) {  // ← 因为没有result_value列，这里为null
        value = BigDecimal.ZERO;  // ← 被赋值为0
    }
    // ...
}
```

## 解决方案

### 修复SQL

为C0002指标项SQL添加 `AS result_value` 别名：

#### 修复后的SQL
```sql
select count(distinct A48,A49) AS result_value
from d_mr
where A14 >= 18
AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN #{startDate} AND #{endDate}
```

### 执行更新
```sql
UPDATE t_indicator_item
SET query_sql = 'select count(distinct A48,A49) AS result_value from d_mr where A14 >= 18 AND STR_TO_DATE(B15, ''%Y/%m/%d'') BETWEEN #{startDate} AND #{endDate}'
WHERE item_code = 'C0002';
```

## 测试验证

### 修复前
```
科室1: 0人
科室2: 0人
科室3: 0人
...
所有科室都是0
```

### 修复后
```
Status: 200
科室数: 19
总和: 84人 ✅（与普通计算一致）

详细结果:
1. 产科: 7人
2. 其他: 9人
3. 妇产科: 4人
4. 儿科: 1人
5. 急诊科室: 2人
6. 呼吸内科: 6人
7. 产房: 4人
8. 康复医学科: 1人
9. 心血管内科: 8人
10. 普通外科: 8人
... (共19个科室)
```

## 影响范围

### 受影响的指标项
- C0002 - 测试指标-成人患者数 ✅ 已修复

### 需要检查的其他指标项
建议检查所有指标项的SQL，确保都包含 `AS result_value` 别名。

### 检查SQL
```sql
SELECT item_code, item_name, query_sql
FROM t_indicator_item
WHERE query_sql NOT LIKE '%AS result_value%'
  AND query_sql NOT LIKE '%as result_value%'
  AND status = 1;
```

## 最佳实践建议

### 1. 指标项SQL规范
所有指标项的SQL必须使用 `AS result_value` 作为结果列的别名：

**正确示例**:
```sql
SELECT COUNT(*) AS result_value FROM ...
SELECT SUM(field) AS result_value FROM ...
SELECT AVG(field) AS result_value FROM ...
SELECT COUNT(DISTINCT A48,A49) AS result_value FROM ...
```

**错误示例**:
```sql
SELECT COUNT(*) FROM ...  ❌ 缺少别名
SELECT SUM(field) as value FROM ...  ❌ 错误的别名
SELECT COUNT(DISTINCT A48,A49) FROM ...  ❌ 缺少别名
```

### 2. 新增指标项检查清单
- [ ] SQL是否包含 `AS result_value`？
- [ ] SQL是否使用参数化查询 `#{startDate}` 和 `#{endDate}`？
- [ ] SQL是否测试通过？
- [ ] 普通计算是否正常？
- [ ] 科室下钻是否正常？

### 3. 代码增强建议（可选）
可以在后端代码中添加更智能的列名检测：

```java
// 改进方案：如果找不到result_value，尝试其他可能的列名
Object valueObj = row.get("result_value");
if (valueObj == null) {
    // 尝试其他可能的列名
    for (String key : row.keySet()) {
        if (key.toLowerCase().contains("count") ||
            key.toLowerCase().contains("sum") ||
            key.toLowerCase().contains("value")) {
            valueObj = row.get(key);
            break;
        }
    }
}
```

但**更好的方案是规范SQL**，确保统一使用 `AS result_value`。

## 总结

### 问题根源
指标项SQL缺少 `AS result_value` 别名，导致后端无法正确读取科室下钻结果。

### 修复方法
为C0002指标项SQL添加 `AS result_value` 别名。

### 修复结果
✅ 科室下钻功能恢复正常
✅ 19个科室总和84人，与普通计算一致
✅ 所有科室都有正确的数值

### 后续建议
1. 检查其他指标项SQL，确保都有 `AS result_value`
2. 建立指标项SQL规范
3. 新增指标项时严格遵守规范

---

**修复时间**: 2026-01-10
**影响指标**: C0002 - 测试指标-成人患者数
**修复状态**: ✅ 完成
**测试状态**: ✅ 通过
