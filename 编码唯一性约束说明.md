# 指标编码与指标项编码唯一性约束说明

## 1. 概述

为了防止指标编码（`metric_code`）和指标项编码（`item_code`）混淆，系统在**数据库层**和**应用层**都实施了严格的唯一性约束。

---

## 2. 数据库层约束

### 2.1 指标项编码唯一性

**表**: `t_indicator_item`
**约束**: `UNIQUE KEY uk_item_code (item_code)`

```sql
CREATE TABLE `t_indicator_item` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `item_code` VARCHAR(50) NOT NULL COMMENT '指标项编码（如 a0050）',
    -- 其他字段...
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_item_code` (`item_code`),  -- ← 唯一性约束
    KEY `idx_item_type` (`item_type`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='指标项表';
```

**作用**：
- 确保每个指标项编码在数据库中全局唯一
- 防止重复插入相同的 `item_code`
- 数据库级别的硬约束，无法绕过

---

### 2.2 指标编码唯一性

**表**: `t_indicator`
**约束**: `UNIQUE KEY uk_metric_code (metric_code)`

```sql
CREATE TABLE `t_indicator` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `metric_code` VARCHAR(50) NOT NULL COMMENT '指标编码（如 10.3.1）',
    -- 其他字段...
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_metric_code` (`metric_code`),  -- ← 唯一性约束
    KEY `idx_parent_code` (`parent_code`),
    KEY `idx_is_leaf` (`is_leaf`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='指标表';
```

**作用**：
- 确保每个指标编码在数据库中全局唯一
- 防止重复插入相同的 `metric_code`
- 数据库级别的硬约束，无法绕过

---

## 3. 应用层校验

### 3.1 指标项编码唯一性校验

**位置**: `IndicatorItemServiceImpl.java` 第 41-49 行

```java
@Override
@Transactional(rollbackFor = Exception.class)
public IndicatorItem saveOrUpdateIndicatorItem(IndicatorItemSaveDTO dto) {
    // 校验指标项编码唯一性
    LambdaQueryWrapper<IndicatorItem> queryWrapper = new LambdaQueryWrapper<>();
    queryWrapper.eq(IndicatorItem::getItemCode, dto.getItemCode());
    if (dto.getId() != null) {
        queryWrapper.ne(IndicatorItem::getId, dto.getId());  // 更新时排除自己
    }
    if (this.count(queryWrapper) > 0) {
        throw new BusinessException("指标项编码已存在：" + dto.getItemCode());
    }

    // ... 后续保存逻辑
}
```

**校验逻辑**：
1. **新增时**：检查是否已存在相同的 `item_code`
2. **更新时**：检查是否存在其他记录使用相同的 `item_code`（排除当前记录）
3. **重复时**：抛出 `BusinessException`，返回友好的错误信息

---

### 3.2 指标编码唯一性校验

**位置**: `IndicatorServiceImpl.java` 第 35-43 行

```java
@Override
@Transactional(rollbackFor = Exception.class)
public Indicator saveOrUpdateIndicator(IndicatorSaveDTO dto) {
    // 1. 校验指标编码唯一性
    LambdaQueryWrapper<Indicator> queryWrapper = new LambdaQueryWrapper<>();
    queryWrapper.eq(Indicator::getMetricCode, dto.getMetricCode());
    if (dto.getId() != null) {
        queryWrapper.ne(Indicator::getId, dto.getId());  // 更新时排除自己
    }
    if (this.count(queryWrapper) > 0) {
        throw new BusinessException("指标编码已存在：" + dto.getMetricCode());
    }

    // ... 后续保存逻辑
}
```

**校验逻辑**：
1. **新增时**：检查是否已存在相同的 `metric_code`
2. **更新时**：检查是否存在其他记录使用相同的 `metric_code`（排除当前记录）
3. **重复时**：抛出 `BusinessException`，返回友好的错误信息

---

## 4. 错误响应示例

### 4.1 指标项编码重复

**请求**：
```javascript
POST /api/indicator-item
{
  "itemCode": "a0050",  // 已存在
  "itemName": "新指标项",
  "itemType": "COLLECTED",
  "status": 1
}
```

**响应**：
```json
{
  "code": 500,
  "message": "指标项编码已存在：a0050",
  "data": null,
  "timestamp": 1704902400000
}
```

---

### 4.2 指标编码重复

**请求**：
```javascript
POST /api/indicator
{
  "metricCode": "急性心肌梗死平均住院日",  // 已存在
  "metricName": "新指标",
  "isLeaf": 1,
  "metricType": "QUANTITATIVE",
  "calculationType": "ITEM",
  "status": 1
}
```

**响应**：
```json
{
  "code": 500,
  "message": "指标编码已存在：急性心肌梗死平均住院日",
  "data": null,
  "timestamp": 1704902400000
}
```

---

## 5. 编码命名规范（避免混淆）

### 5.1 指标项编码规范

**格式**: 小写字母 + 数字
**示例**: `a0050`, `a0052`, `b0001`

**特点**：
- 短小精悍，通常 5-10 个字符
- 纯代码形式，不包含中文
- 用于SQL查询和计算表达式中
- 建议前缀分类：
  - `a` 开头：基础数据项
  - `b` 开头：业务数据项
  - `c` 开头：计算数据项

**示例**：
```javascript
{
  "itemCode": "a0050",
  "itemName": "肺炎病种例数",
  "querySql": "SELECT COUNT(*) AS result_value FROM d_mr WHERE B02 LIKE '%肺炎%'"
}
```

---

### 5.2 指标编码规范

**格式**: 中文名称 或 分级编号（如 10.3.1）

**示例**:
- 中文名称：`急性心肌梗死平均住院日`、`肺炎平均住院日`
- 分级编号：`10.3.1`、`10.3.2`、`99.1`

**特点**：
- 可读性强，直观描述指标含义
- 用于业务层面的标识和展示
- 建议使用分级编号或有意义的中文名称

**示例**：
```javascript
{
  "metricCode": "急性心肌梗死平均住院日",
  "metricName": "急性心肌梗死平均住院日",
  "expression": "a0034/a0032",
  "relatedItems": "[\"a0034\",\"a0032\"]"
}
```

---

## 6. 两者的关系与区别

| 项目 | 指标项（IndicatorItem） | 指标（Indicator） |
|------|------------------------|------------------|
| **编码字段** | `item_code` | `metric_code` |
| **编码示例** | `a0050`, `a0052` | `急性心肌梗死平均住院日`, `10.3.1` |
| **作用** | 最小数据采集单元 | 业务指标，可由多个指标项计算得出 |
| **SQL查询** | 包含实际的SQL语句 | 包含计算表达式（引用指标项） |
| **是否可计算** | 执行SQL获取原始数据 | 通过表达式计算最终结果 |
| **关系** | 被引用 | 引用指标项 |

**关系示例**：
```javascript
// 指标项 a0034（心肌梗死总住院日数）
{
  "itemCode": "a0034",
  "itemName": "急性心肌梗死总住院日",
  "querySql": "SELECT SUM(B07) AS result_value FROM d_mr WHERE B02='I21.000'"
}

// 指标项 a0032（心肌梗死病种例数）
{
  "itemCode": "a0032",
  "itemName": "急性心肌梗死病种例数",
  "querySql": "SELECT COUNT(*) AS result_value FROM d_mr WHERE B02='I21.000'"
}

// 指标（使用上述两个指标项）
{
  "metricCode": "急性心肌梗死平均住院日",
  "metricName": "急性心肌梗死平均住院日",
  "expression": "a0034/a0032",  // ← 引用指标项编码
  "relatedItems": "[\"a0034\",\"a0032\"]",
  "unit": "天"
}
```

---

## 7. 前端开发注意事项

### 7.1 表单校验建议

```vue
<template>
  <el-form :model="form" :rules="rules">
    <!-- 指标项编码输入 -->
    <el-form-item label="指标项编码" prop="itemCode">
      <el-input
        v-model="form.itemCode"
        placeholder="请输入指标项编码（如：a0050）"
        :maxlength="50"
      />
      <div class="hint">
        建议格式：小写字母+数字，如 a0050、b0001
      </div>
    </el-form-item>

    <!-- 指标编码输入 -->
    <el-form-item label="指标编码" prop="metricCode">
      <el-input
        v-model="form.metricCode"
        placeholder="请输入指标编码（如：急性心肌梗死平均住院日）"
        :maxlength="50"
      />
      <div class="hint">
        建议使用中文名称或分级编号（如：10.3.1）
      </div>
    </el-form-item>
  </el-form>
</template>

<script setup>
import { reactive } from 'vue';

const rules = {
  itemCode: [
    { required: true, message: '请输入指标项编码', trigger: 'blur' },
    {
      pattern: /^[a-z][a-z0-9]{2,49}$/,
      message: '指标项编码格式：小写字母开头+数字，3-50位',
      trigger: 'blur'
    }
  ],
  metricCode: [
    { required: true, message: '请输入指标编码', trigger: 'blur' },
    { max: 50, message: '指标编码长度不能超过50个字符', trigger: 'blur' }
  ]
};
</script>
```

---

### 7.2 错误处理建议

```javascript
const createIndicatorItem = async (data) => {
  try {
    const response = await fetch('http://localhost:8080/dgear/api/indicator-item', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data)
    });
    const result = await response.json();

    if (result.code === 200) {
      ElMessage.success('创建成功');
      return result.data;
    } else {
      // 处理唯一性约束错误
      if (result.message.includes('编码已存在')) {
        ElMessage.error({
          message: result.message,
          duration: 5000,  // 显示更长时间
          showClose: true
        });
      } else {
        ElMessage.error(result.message);
      }
      throw new Error(result.message);
    }
  } catch (error) {
    console.error('创建指标项失败:', error);
    throw error;
  }
};
```

---

### 7.3 编码查重功能（可选）

```vue
<template>
  <el-form-item label="指标项编码" prop="itemCode">
    <el-input
      v-model="form.itemCode"
      @blur="checkItemCodeUnique"
    >
      <template #suffix>
        <el-icon v-if="checking" class="is-loading">
          <Loading />
        </el-icon>
        <el-icon v-else-if="codeExists" color="red">
          <CircleClose />
        </el-icon>
        <el-icon v-else-if="codeChecked" color="green">
          <CircleCheck />
        </el-icon>
      </template>
    </el-input>
    <div v-if="codeExists" class="error-hint">
      该编码已存在，请更换
    </div>
  </el-form-item>
</template>

<script setup>
import { ref } from 'vue';

const checking = ref(false);
const codeExists = ref(false);
const codeChecked = ref(false);

const checkItemCodeUnique = async () => {
  if (!form.itemCode) return;

  checking.value = true;
  codeExists.value = false;
  codeChecked.value = false;

  try {
    const response = await fetch(
      `http://localhost:8080/dgear/api/indicator-item/code/${form.itemCode}`
    );
    const result = await response.json();

    if (result.code === 200 && result.data) {
      // 编辑模式下，如果是当前记录，则不算重复
      if (!isEditMode || result.data.id !== currentId) {
        codeExists.value = true;
      } else {
        codeChecked.value = true;
      }
    } else {
      codeChecked.value = true;
    }
  } catch (error) {
    console.error('检查编码失败:', error);
  } finally {
    checking.value = false;
  }
};
</script>
```

---

## 8. 测试用例

### 8.1 测试指标项编码唯一性

```javascript
// 测试1：创建第一个指标项
const item1 = await fetch('http://localhost:8080/dgear/api/indicator-item', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    itemCode: 'test_unique_001',
    itemName: '测试指标项1',
    itemType: 'COLLECTED',
    status: 1
  })
}).then(res => res.json());

console.log('创建成功:', item1);
// 预期：成功，返回 code: 200

// 测试2：尝试创建相同编码的指标项
const item2 = await fetch('http://localhost:8080/dgear/api/indicator-item', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    itemCode: 'test_unique_001',  // 相同编码
    itemName: '测试指标项2',
    itemType: 'COLLECTED',
    status: 1
  })
}).then(res => res.json());

console.log('创建失败:', item2);
// 预期：失败，返回 code: 500, message: "指标项编码已存在：test_unique_001"

// 测试3：更新指标项（保持自己的编码）
const item3 = await fetch(`http://localhost:8080/dgear/api/indicator-item/${item1.data.id}`, {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    itemCode: 'test_unique_001',  // 保持原编码
    itemName: '测试指标项1-已更新',
    itemType: 'COLLECTED',
    status: 1
  })
}).then(res => res.json());

console.log('更新成功:', item3);
// 预期：成功，返回 code: 200

// 清理测试数据
await fetch(`http://localhost:8080/dgear/api/indicator-item/${item1.data.id}`, {
  method: 'DELETE'
});
```

---

### 8.2 测试指标编码唯一性

```javascript
// 测试1：创建第一个指标
const indicator1 = await fetch('http://localhost:8080/dgear/api/indicator', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '测试指标-唯一性001',
    metricName: '测试指标1',
    isLeaf: 1,
    indicatorLevel: 1,
    metricType: 'QUANTITATIVE',
    calculationType: 'ITEM',
    relatedItems: '["test_unique_001"]',
    status: 1
  })
}).then(res => res.json());

console.log('创建成功:', indicator1);
// 预期：成功，返回 code: 200

// 测试2：尝试创建相同编码的指标
const indicator2 = await fetch('http://localhost:8080/dgear/api/indicator', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    metricCode: '测试指标-唯一性001',  // 相同编码
    metricName: '测试指标2',
    isLeaf: 1,
    indicatorLevel: 1,
    metricType: 'QUANTITATIVE',
    calculationType: 'ITEM',
    relatedItems: '["test_unique_001"]',
    status: 1
  })
}).then(res => res.json());

console.log('创建失败:', indicator2);
// 预期：失败，返回 code: 500, message: "指标编码已存在：测试指标-唯一性001"

// 清理测试数据
await fetch(`http://localhost:8080/dgear/api/indicator/${indicator1.data.id}`, {
  method: 'DELETE'
});
```

---

## 9. 常见问题

### Q1: 如果我想修改已有记录的编码怎么办？

**答**: 可以直接修改，只要新编码不与其他记录冲突即可。系统会自动排除当前记录本身。

```javascript
// 更新指标项编码（从 a0050 改为 a0051）
PUT /api/indicator-item/123
{
  "itemCode": "a0051",  // 新编码
  "itemName": "原指标项名称",
  // ... 其他字段保持不变
}
```

---

### Q2: 指标项编码和指标编码可以相同吗？

**答**: 技术上可以，因为它们存储在不同的表中，有独立的唯一性约束。但**强烈不建议**这样做，因为：

1. 容易造成混淆
2. 在表达式中引用时会产生歧义
3. 不符合最佳实践

**建议做法**：
- 指标项编码：使用短代码（如 `a0050`）
- 指标编码：使用中文名称或分级编号（如 `急性心肌梗死平均住院日`）

---

### Q3: 如果数据库中已有重复数据，如何处理？

**答**: 理论上不应该存在，因为有数据库约束。如果真的存在（如手动SQL绕过约束），需要手动清理：

```sql
-- 查找重复的指标项编码
SELECT item_code, COUNT(*) as count
FROM t_indicator_item
GROUP BY item_code
HAVING count > 1;

-- 保留最新的一条，删除旧数据
DELETE FROM t_indicator_item
WHERE id NOT IN (
    SELECT MAX(id) FROM t_indicator_item GROUP BY item_code
);

-- 查找重复的指标编码
SELECT metric_code, COUNT(*) as count
FROM t_indicator
GROUP BY metric_code
HAVING count > 1;

-- 保留最新的一条，删除旧数据
DELETE FROM t_indicator
WHERE id NOT IN (
    SELECT MAX(id) FROM t_indicator GROUP BY metric_code
);
```

---

## 10. 总结

✅ **系统已具备完善的唯一性保护**：
1. **数据库层**：UNIQUE KEY 约束，硬性保证唯一性
2. **应用层**：Service 层校验，提供友好的错误提示
3. **双重保障**：即使应用层校验被绕过，数据库层仍能拦截

✅ **防止混淆的建议**：
1. 指标项编码使用短代码（如 `a0050`）
2. 指标编码使用中文名称或分级编号（如 `急性心肌梗死平均住院日`）
3. 前端表单添加格式校验和实时查重
4. 在文档和命名规范中明确区分

✅ **用户体验优化**：
- 错误提示清晰明确："指标项编码已存在：a0050"
- 可在前端添加实时查重功能
- 提供编码命名建议和示例

---

**文档版本**: 1.0
**创建时间**: 2026-01-09
**最后更新**: 2026-01-09
