# 科室下钻接口真实可用示例

## ✅ 真实接口信息

**接口路径**: `POST /api/indicator-result/dept-drill-down`

**参数方式**: Query Parameters（URL参数，不是JSON Body）

**完整URL**: `http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down`

---

## 🚀 示例1: 剖宫产率科室下钻（单指标项）

### curl 命令
```bash
curl -X POST "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down?metricCode=%E5%89%96%E5%AE%AB%E4%BA%A7%E7%8E%87&timeDimension=year&startDate=2020-01-01&endDate=2020-12-31"
```

### Postman 配置
```
Method: POST
URL: http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down

Params (Query Parameters):
  metricCode = 剖宫产率
  timeDimension = year
  startDate = 2020-01-01
  endDate = 2020-12-31
```

### Python 代码
```python
import requests

url = "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down"
params = {
    "metricCode": "剖宫产率",
    "timeDimension": "year",
    "startDate": "2020-01-01",
    "endDate": "2020-12-31"
}

response = requests.post(url, params=params)
print(response.json())
```

### 成功响应示例
```json
{
  "code": 200,
  "message": "科室下钻计算完成，共计算 15 个科室",
  "data": [
    {
      "id": 1,
      "metricCode": "剖宫产率",
      "timeDimension": "year",
      "timeValue": "2020",
      "deptCode": "科室001",
      "deptName": "妇产科",
      "resultValue": 450.0000,
      "resultJson": "{...}",
      "calculationStatus": "SUCCESS",
      "errorMessage": null,
      "createTime": "2026-01-10T17:00:00",
      "updateTime": "2026-01-10T17:00:00"
    }
    // ... 其他科室
  ]
}
```

---

## 🚀 示例2: 急性心肌梗死住院病死率科室下钻（表达式）

### curl 命令
```bash
curl -X POST "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down?metricCode=%E5%8D%95%E7%97%85%E7%A7%8D%E8%B4%A8%E9%87%8F%E6%8E%A7%E5%88%B6%E6%8C%87%E6%A0%87-%E6%80%A5%E6%80%A7%E5%BF%83%E8%82%8C%E6%A2%97%E6%AD%BB%E6%82%A3%E8%80%85%E4%BD%8F%E9%99%A2%E7%97%85%E6%AD%BB%E7%8E%87&timeDimension=year&startDate=2020-01-01&endDate=2020-12-31"
```

### Postman 配置
```
Method: POST
URL: http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down

Params:
  metricCode = 单病种质量控制指标-急性心肌梗死患者住院病死率
  timeDimension = year
  startDate = 2020-01-01
  endDate = 2020-12-31
```

### Python 代码
```python
import requests

url = "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down"
params = {
    "metricCode": "单病种质量控制指标-急性心肌梗死患者住院病死率",
    "timeDimension": "year",
    "startDate": "2020-01-01",
    "endDate": "2020-12-31"
}

response = requests.post(url, params=params, timeout=60)
result = response.json()

if result['code'] == 200:
    print(f"成功! 计算了 {len(result['data'])} 个科室")
    for item in result['data'][:5]:  # 显示前5个
        print(f"科室: {item['deptName']}, 结果值: {item['resultValue']}")
else:
    print(f"失败: {result['message']}")
```

---

## 📊 参数说明

### 请求参数（Query Parameters）

| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| metricCode | String | 是 | 指标编码 | 剖宫产率 |
| timeDimension | String | 是 | 时间维度 | year/quarter/month/day |
| startDate | String | 是 | 开始日期 | 2020-01-01 |
| endDate | String | 是 | 结束日期 | 2020-12-31 |

### 时间维度取值

| 值 | 说明 | 日期范围示例 |
|----|------|-------------|
| year | 按年 | 2020-01-01 至 2020-12-31 |
| quarter | 按季度 | 2020-01-01 至 2020-03-31（第1季度） |
| month | 按月 | 2020-01-01 至 2020-01-31 |
| day | 按天 | 2020-01-01 至 2020-01-01 |

---

##🔍 查看执行结果的方法

### 方法1: 直接查看接口返回的data数组

成功响应中的 `data` 数组就是所有科室的下钻结果。

### 方法2: 查看数据库表

```sql
-- 查看科室下钻结果
SELECT
    metric_code,
    dept_code,
    dept_name,
    result_value,
    calculation_status,
    time_value,
    create_time
FROM t_indicator_result_dept
WHERE metric_code = '剖宫产率'
  AND time_value = '2020'
ORDER BY result_value DESC
LIMIT 10;
```

### 方法3: 查看日志

```bash
# SSH 登录服务器
ssh root@81.71.44.180

# 查看 SQL 日志（应该看到 GROUP BY B16）
tail -f /data/indicator-management/logs/sql.log

# 查看应用日志
tail -f /data/indicator-management/logs/application.log

# 查看错误日志
tail -f /data/indicator-management/logs/error.log
```

---

## ⚠️ 常见错误和解决方案

### 错误1: 404 Not Found

**原因**: 接口路径错误

❌ 错误路径: `/api/indicator-calculation/dept-drill`
✅ 正确路径: `/api/indicator-result/dept-drill-down`

### 错误2: 400 Bad Request - Required parameter 'xxx' is not present

**原因**: 参数没有通过 URL Query String 传递

❌ 错误方式（JSON Body）:
```bash
curl -X POST http://... \
  -H "Content-Type: application/json" \
  -d '{"metricCode":"..."}'
```

✅ 正确方式（URL参数）:
```bash
curl -X POST "http://...?metricCode=xxx&timeDimension=year&..."
```

### 错误3: 返回空数组或计算失败

**可能原因**:
1. 表中B16字段为空
2. 时间范围内没有数据
3. 指标项SQL有错误

**排查步骤**:
```bash
# 1. 查看错误日志
tail -100 /data/indicator-management/logs/error.log

# 2. 查看 SQL 日志，确认是否执行了 GROUP BY B16
grep "GROUP BY B16" /data/indicator-management/logs/sql.log | tail -5

# 3. 检查数据库
mysql -h gz-cdb-bq7gk3k5.sql.tencentcdb.com -P 63606 -u root -pYiguo9527_ \
  d_hos_claude_0251230 -e "
SELECT B16, COUNT(*)
FROM d_mr
WHERE B16 IS NOT NULL
  AND B16 != ''
  AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN '2020-01-01' AND '2020-12-31'
GROUP BY B16
LIMIT 5;"
```

---

## 📝 完整Python测试脚本

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""科室下钻接口测试脚本"""

import requests
import json

def test_dept_drill():
    """测试科室下钻功能"""

    # 配置
    base_url = "http://81.71.44.180:8080/dgear"
    endpoint = "/api/indicator-result/dept-drill-down"

    # 测试参数
    params = {
        "metricCode": "剖宫产率",
        "timeDimension": "year",
        "startDate": "2020-01-01",
        "endDate": "2020-12-31"
    }

    print("="*80)
    print("科室下钻接口测试")
    print("="*80)
    print(f"\n接口URL: {base_url}{endpoint}")
    print(f"参数: {json.dumps(params, ensure_ascii=False, indent=2)}")
    print("\n执行请求...")

    try:
        # 发送请求
        response = requests.post(
            f"{base_url}{endpoint}",
            params=params,
            timeout=60
        )

        print(f"响应状态码: {response.status_code}\n")

        if response.status_code == 200:
            result = response.json()

            if result.get('code') == 200:
                data = result.get('data', [])
                message = result.get('message', '')

                print(f"✓ 成功! {message}")
                print(f"✓ 返回科室数量: {len(data)}\n")

                if data:
                    print("前5个科室的结果:")
                    print("-" * 80)
                    print(f"{'科室编码':<15} {'科室名称':<20} {'结果值':<15} {'状态':<10}")
                    print("-" * 80)

                    for item in data[:5]:
                        dept_code = item.get('deptCode', 'N/A')
                        dept_name = item.get('deptName', 'N/A')
                        result_value = item.get('resultValue', 0)
                        status = item.get('calculationStatus', 'N/A')
                        print(f"{dept_code:<15} {dept_name:<20} {result_value:<15.4f} {status:<10}")

                    if len(data) > 5:
                        print(f"\n... 还有 {len(data) - 5} 个科室")
                else:
                    print("✗ 未返回任何科室数据")
                    print("可能原因:")
                    print("  1. B16字段为空")
                    print("  2. 时间范围内没有数据")
                    print("  3. SQL执行失败")
                    print("\n请查看日志:")
                    print("  tail -f /data/indicator-management/logs/error.log")

            else:
                print(f"✗ 业务错误: {result.get('message')}")
        else:
            print(f"✗ HTTP错误: {response.status_code}")
            print(f"响应内容: {response.text[:500]}")

    except Exception as e:
        print(f"✗ 异常: {e}")

if __name__ == "__main__":
    test_dept_drill()
```

保存为 `test_dept_drill.py`，然后运行:
```bash
python test_dept_drill.py
```

---

## 🎯 总结

### 关键信息

1. **正确的接口路径**: `/api/indicator-result/dept-drill-down`（注意是 `result` 不是 `calculation`）
2. **参数方式**: URL Query Parameters（不是 JSON Body）
3. **请求方法**: POST
4. **返回格式**: `{code, message, data[]}`，data数组包含所有科室的结果

### 快速测试命令

```bash
# 最简单的测试方式
curl -X POST "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down?metricCode=剖宫产率&timeDimension=year&startDate=2020-01-01&endDate=2020-12-31"
```

### 验证步骤

1. ✅ 发送POST请求到正确的接口
2. ✅ 使用URL参数传递 metricCode, timeDimension, startDate, endDate
3. ✅ 检查响应中的data数组（每个科室一条记录）
4. ✅ 查看日志确认SQL中有 `GROUP BY B16`
5. ✅ 查询数据库表 `t_indicator_result_dept` 验证数据已保存

按照这个示例操作，一定能成功获取科室下钻结果！
