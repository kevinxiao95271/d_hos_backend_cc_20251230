# 指标科室下钻接口使用示例

## 📌 完整流程说明

### 前置条件
1. 指标必须是**叶子节点**（`isLeaf = 1`）
2. 指标必须配置了**关联指标项**（`relatedItems` 不为空）
3. 指标项的 SQL 必须查询**包含 B16 字段的表**（科室字段）

---

## 🚀 示例1: 单指标项科室下钻

### 指标信息
- **指标编码**: `剖宫产率`
- **指标名称**: `剖宫产率`
- **计算类型**: `ITEM`（单指标项，无表达式）
- **关联指标项**: `["a0032"]`
- **说明**: 虽然是单指标项，但**同样可以科室下钻**

### API 调用

#### 接口地址
```
POST http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down
```

#### 请求方式
URL Query Parameters（URL参数，不是JSON Body）

#### 请求参数（通过URL传递）
```
metricCode=剖宫产率
timeDimension=year
startDate=2020-01-01
endDate=2020-12-31
```

#### 成功响应示例
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1001,
      "metricCode": "剖宫产率",
      "timeDimension": "year",
      "timeValue": "2020",
      "deptCode": "01",
      "deptName": "妇产科",
      "resultValue": 450,
      "resultJson": "{\"metric_code\":\"剖宫产率\",\"dept_code\":\"01\",\"dept_name\":\"妇产科\",\"result_value\":450,...}",
      "calculationStatus": "SUCCESS",
      "errorMessage": null,
      "createTime": "2026-01-10 16:30:00",
      "updateTime": "2026-01-10 16:30:00"
    },
    {
      "id": 1002,
      "metricCode": "剖宫产率",
      "timeDimension": "year",
      "timeValue": "2020",
      "deptCode": "02",
      "deptName": "产科",
      "resultValue": 380,
      "resultJson": "{...}",
      "calculationStatus": "SUCCESS",
      "errorMessage": null,
      "createTime": "2026-01-10 16:30:00",
      "updateTime": "2026-01-10 16:30:00"
    }
    // ... 每个科室一条记录
  ]
}
```

#### curl 命令
```bash
curl -X POST "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down?metricCode=%E5%89%96%E5%AE%AB%E4%BA%A7%E7%8E%87&timeDimension=year&startDate=2020-01-01&endDate=2020-12-31"
```

---

## 🚀 示例2: 表达式指标科室下钻

### 指标信息
- **指标编码**: `单病种质量控制指标-急性心肌梗死患者住院病死率`
- **指标名称**: `单病种质量控制指标-急性心肌梗死患者住院病死率`
- **计算类型**: `EXPRESSION`（表达式计算）
- **表达式**: `a0027/a0329`
- **关联指标项**: `["a0027", "a0329"]`
- **说明**: 表达式指标会**自动计算每个科室的比率**

### API 调用

#### 请求体
```bash
curl -X POST "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down?metricCode=%E5%8D%95%E7%97%85%E7%A7%8D%E8%B4%A8%E9%87%8F%E6%8E%A7%E5%88%B6%E6%8C%87%E6%A0%87-%E6%80%A5%E6%80%A7%E5%BF%83%E8%82%8C%E6%A2%97%E6%AD%BB%E6%82%A3%E8%80%85%E4%BD%8F%E9%99%A2%E7%97%85%E6%AD%BB%E7%8E%87&timeDimension=year&startDate=2020-01-01&endDate=2020-12-31"
```

#### 成功响应示例
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 2001,
      "metricCode": "单病种质量控制指标-急性心肌梗死患者住院病死率",
      "timeDimension": "year",
      "timeValue": "2020",
      "deptCode": "03",
      "deptName": "心内科",
      "resultValue": 0.0523,
      "resultJson": "{\"metric_code\":\"...\",\"expression\":\"a0027/a0329\",\"item_values\":{\"a0027\":12,\"a0329\":230},...}",
      "calculationStatus": "SUCCESS",
      "errorMessage": null,
      "createTime": "2026-01-10 16:35:00",
      "updateTime": "2026-01-10 16:35:00"
    },
    {
      "id": 2002,
      "metricCode": "单病种质量控制指标-急性心肌梗死患者住院病死率",
      "timeDimension": "year",
      "timeValue": "2020",
      "deptCode": "04",
      "deptName": "急诊科",
      "resultValue": 0.0856,
      "resultJson": "{...}",
      "calculationStatus": "SUCCESS",
      "errorMessage": null,
      "createTime": "2026-01-10 16:35:00",
      "updateTime": "2026-01-10 16:35:00"
    }
  ]
}
```

#### curl 命令
```bash
curl -X POST "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill-down?metricCode=%E5%8D%95%E7%97%85%E7%A7%8D%E8%B4%A8%E9%87%8F%E6%8E%A7%E5%88%B6%E6%8C%87%E6%A0%87-%E6%80%A5%E6%80%A7%E5%BF%83%E8%82%8C%E6%A2%97%E6%AD%BB%E6%82%A3%E8%80%85%E4%BD%8F%E9%99%A2%E7%97%85%E6%AD%BB%E7%8E%87&timeDimension=year&startDate=2020-01-01&endDate=2020-12-31"
```

---

## 🔍 示例3: 查询已保存的科室下钻结果

### 说明
科室下钻计算完成后，结果会保存到数据库。可以通过此接口查询历史数据，无需重新计算。

### API 调用

#### 接口地址
```
GET http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill/{metricCode}
```

#### 请求参数
```
路径参数:
  metricCode = 剖宫产率

查询参数 (可选):
  timeDimension = year
  timeValue = 2020
```

#### 完整URL
```
http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill/剖宫产率?timeDimension=year&timeValue=2020
```

#### 成功响应示例
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1001,
      "metricCode": "剖宫产率",
      "timeDimension": "year",
      "timeValue": "2020",
      "deptCode": "01",
      "deptName": "妇产科",
      "resultValue": 450,
      "resultJson": "{...}",
      "calculationStatus": "SUCCESS",
      "errorMessage": null,
      "createTime": "2026-01-10 16:30:00",
      "updateTime": "2026-01-10 16:30:00"
    }
    // ... 其他科室的历史记录
  ]
}
```

#### curl 命令
```bash
curl -X GET "http://81.71.44.180:8080/dgear/api/indicator-result/dept-drill/%E5%89%96%E5%AE%AB%E4%BA%A7%E7%8E%87?timeDimension=year&timeValue=2020"
```

---

## 📊 参数说明

### 请求参数

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| metricCode | String | 是 | 指标编码 | "剖宫产率" |
| timeDimension | String | 是 | 时间维度 | "year", "quarter", "month" |
| startDate | String | 是 | 开始日期 | "2020-01-01" |
| endDate | String | 是 | 结束日期 | "2020-12-31" |

### 时间维度说明

| timeDimension | 说明 | 日期范围示例 |
|--------------|------|-------------|
| year | 按年统计 | 2020-01-01 至 2020-12-31 |
| quarter | 按季度统计 | 2020-01-01 至 2020-03-31（Q1） |
| month | 按月统计 | 2020-01-01 至 2020-01-31 |

### 响应字段

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 记录ID |
| metricCode | String | 指标编码 |
| timeDimension | String | 时间维度 |
| timeValue | String | 时间值（如 "2020"） |
| deptCode | String | 科室编码 |
| deptName | String | 科室名称 |
| resultValue | BigDecimal | 计算结果值 |
| resultJson | String | 完整结果JSON（包含所有指标项的值） |
| calculationStatus | String | 计算状态：SUCCESS/FAILED |
| errorMessage | String | 错误信息（失败时） |
| createTime | String | 创建时间 |
| updateTime | String | 更新时间 |

---

## ⚠️ 常见问题

### 1. 为什么调用后返回空数组？

**可能原因**:
- ❌ 数据库表中 B16 字段值为空
- ❌ 时间范围内没有数据
- ❌ 指标项 SQL 有误

**排查方法**:
```sql
-- 检查表中是否有科室数据
SELECT B16, COUNT(*)
FROM d_mr
WHERE B16 IS NOT NULL
  AND B16 != ''
  AND STR_TO_DATE(B15, '%Y/%m/%d') BETWEEN '2020-01-01' AND '2020-12-31'
GROUP BY B16;
```

**查看日志**:
```bash
# 查看 SQL 执行日志
tail -f /data/indicator-management/logs/sql.log | grep "GROUP BY B16"

# 查看错误日志
tail -f /data/indicator-management/logs/error.log
```

### 2. 单指标项（无表达式）能否下钻？

**回答**: ✅ **可以！**

- 科室下钻与是否有表达式**无关**
- 关键是指标项的 SQL 能查询到 B16 字段
- 系统会自动将 SQL 改写为：
  ```sql
  SELECT B16 as dept_code, B16 as dept_name, COUNT(*) as result_value
  FROM d_mr
  WHERE ...
  GROUP BY B16
  ```

### 3. 如何验证下钻功能正常？

**步骤**:

1. 调用科室下钻接口
2. 查看 SQL 日志，确认有 `GROUP BY B16` 的 SQL
3. 查看响应，应该返回多条记录（每个科室一条）
4. 查询数据库表 `t_indicator_result_dept`

```sql
SELECT dept_code, dept_name, result_value
FROM t_indicator_result_dept
WHERE metric_code = '剖宫产率'
  AND time_value = '2020'
ORDER BY result_value DESC;
```

### 4. 普通计算 vs 科室下钻的区别

| 对比项 | 普通计算 | 科室下钻 |
|--------|---------|---------|
| 接口 | `/api/indicator-result/calculate` | `/api/indicator-result/dept-drill-down` |
| 返回结果 | 1条汇总记录 | N条记录（每个科室一条） |
| 数据库表 | `t_indicator_result` | `t_indicator_result_dept` |
| SQL | 原始 SQL | 自动添加 `GROUP BY B16` |
| 示例 | `总病案数 = 10000` | `内科=1500, 外科=1200, ...` |
| 查询接口 | `GET /api/indicator-result/list` | `GET /api/indicator-result/dept-drill/{metricCode}` |

---

## 🧪 运行 Python 测试脚本

```bash
# 安装依赖
pip install requests

# 运行测试脚本
python test_dept_drill_api.py
```

脚本会自动执行以下测试：
1. 单指标项科室下钻
2. 表达式指标科室下钻
3. 查询历史下钻结果
4. 显示等效的 curl 命令

---

## 📝 总结

### 关键要点

1. ✅ **必须调用专用接口**: `/api/indicator-result/dept-drill-down`（执行下钻）
2. ✅ **查询接口**: `GET /api/indicator-result/dept-drill/{metricCode}`（查询结果）
3. ✅ **参数方式**: URL参数，不是JSON Body
4. ✅ **单指标项也可以下钻**: 不需要表达式
5. ✅ **关键是 B16 字段**: 表必须包含科室字段
6. ✅ **结果单独存储**: `t_indicator_result_dept` 表
7. ✅ **可重复查询**: 结果保存后可通过查询接口获取

### 使用建议

- 优先使用真实存在的指标进行测试（如"剖宫产率"）
- 选择有数据的时间范围（2020年数据较全）
- 通过日志确认 SQL 执行情况
- 验证数据库表中是否有结果记录
